starting with `x`.

### Insight
- If `n` is even, the +x and –x terms cancel → total = 0
- If `n` is odd, one extra +x remains → total = x

### Files
- `mystic_waves.py` — optimized Python solution
- `input.txt` - txt file that contains pre-entered input 

### How to Run
1. cd A
2. python3 mystic_waves.py < input.txt

